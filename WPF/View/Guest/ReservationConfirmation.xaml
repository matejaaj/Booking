<Window x:Class="BookingApp.WPF.View.Guest.ReservationConfirmation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.View.Guest"
        mc:Ignorable="d"
        Title="ReservationConfirmation" Height="140" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Enter number of people for reservation:" Margin="10"/>
        <TextBox Grid.Row="1" x:Name="NumberOfPeopleTextBox" Margin="90,10,90,10" Text="{Binding GuestNumber}" />
        <Button Grid.Row="2" Content="Confirm" Click="ConfirmReservationButton_Click" Margin="10"/>
    </Grid>
</Window>
