<Window x:Class="BookingApp.WPF.View.Tourist.TouristTabsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.View.Tourist"
        xmlns:control="clr-namespace:BookingApp.WPF.View.Tourist.UserControls"
        mc:Ignorable="d"
        Title="TouristTabsWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="TabStyle" TargetType="TabItem">
            <Setter Property="Width" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" TabStripPlacement="Left">
            <TabItem Header="Ture" Style="{StaticResource TabStyle}">
                <TabControl>
                    <TabItem Header="Sve ture">

                    </TabItem>
                    <TabItem Header="Moje ture">
                        <control:MyToursControl DataContext="{Binding ToursMainViewModel.MyToursViewModel}"
                                                Tourist="{Binding Tourist}"/>
                    </TabItem>

                    <TabItem Header="Zahtev ture">
                    </TabItem>
                </TabControl>
            </TabItem>
            <TabItem Header="Taksi" Style="{StaticResource TabStyle}">
                <TabControl>

                    <TabItem Header="Moje vožnje">
                    </TabItem>
                </TabControl>
            </TabItem>
        </TabControl>

        <!-- Buttons at the bottom -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Left" Margin="3,-71,0,0">
            <Button Content="Promeni jezik"  />
            <Button Content="Promeni temu" />
            <Button Content="Odjavi se" />
        </StackPanel>
    </Grid>
</Window>
