<Window x:Class="BookingApp.View.Tourist.TourReservationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.Tourist"
        mc:Ignorable="d"
        Title="TourReservationWindow" Height="500" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="cmbStartTime" SelectionChanged="CmbStartTime_SelectionChanged" Grid.Row="0" Margin="10"/>

        <ComboBox x:Name="cmbNumberOfPeople" Grid.Row="1" Margin="10" SelectionChanged="CmbNumberOfPeople_SelectionChanged">
            <ComboBoxItem Content="1"/>
            <ComboBoxItem Content="2"/>
            <ComboBoxItem Content="3"/>
        </ComboBox>



        <ScrollViewer Grid.Row="2" Margin="10">
            <StackPanel x:Name="spPersonInputs" />
        </ScrollViewer>

        <ComboBox x:Name="cmbVoucher" Grid.Row="3" Margin="10"/>

        <Button x:Name="btnSubmit" Content="Submit" Grid.Row="4" Margin="10" Click="BtnSubmit_Click"/>
    </Grid>
</Window>
